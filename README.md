# StorePilot

Simulatore finanziario 4-wall per valutare la sostenibilità economica di location food retail (ricavi, EBITDA, break-even, ROI).

## Quickstart

```bash
python -m venv venv
source venv/bin/activate
# Windows: venv\\Scripts\\activate
pip install -r requirements.txt
streamlit run app.py
```
